import { createNativeStackNavigator } from "@react-navigation/native-stack";
import { LoginPramList } from "../types/types";

const AccessNavigator = createNativeStackNavigator<LoginPramList>();

export default AccessNavigator;
